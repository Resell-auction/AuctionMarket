server:
  port: 8080  # 애플리케이션 포트

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auction_test?serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # 개발 환경에서 사용, 배포 시에는 'none' 또는 'validate'로 설정
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret:
    key: bXktc3VwZXItc2VjcmV0LWtleS1mb3Itand0IQbXktc3VwZXItc2VjcmV0LWtleS1mb3Itand0IQbXktc3VwZXItc2VjcmV0LWtleS1mb3Itand0IQbXktc3VwZXItc2VjcmV0LWtleS1mb3Itand0IQ  # Base64로 인코딩된 JWT secret key
# 추가적인 설정
logging:
  level:
    org.springframework.web: DEBUG  # 디버깅 용 로그 레벨 설정

WEBSOCKET:
  SERVER:
    URL: http://localhost:8081
# 프로파일 관련 설정 (예: dev, prod 등)

cloud:
  aws:
    credentials:
      access-key: AKIASCETYQLIZGVBSEHC
      secret-key: 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  # 시크릿 키도 필요할 가능성이 높음
    region:
      static: ap-northeast-2         # 사용하는 AWS 리전 입력 (예: 서울 리전)
    stack:
      auto: false                    # 자격증명을 직접 제공할 때 auto 설정을 false로 하는 경우가 많음